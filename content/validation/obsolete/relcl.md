---
title: mod:relcl
request: pattern { M -[mod:relcl]-> N }
scope:
  schema: SUD
level: error
type: valid
---

In SUD, `relcl` is a deep edge feature, `mod@relcl` must be used instead of `mod:relcl`.